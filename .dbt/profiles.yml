coin_data_project:
  target: dev
  outputs:
    dev:   # Windows local run
      type: postgres
      host: localhost
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: staging

    docker:   # Airflow container run
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: staging
    
